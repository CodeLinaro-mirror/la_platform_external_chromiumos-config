# This is the vpython spec used by the recipe engine *only*.
# In particular, the python modules in this git repo use Python 3.X,
# whereas the recipe engine still uses Python 2.7
python_version: "2.7"
